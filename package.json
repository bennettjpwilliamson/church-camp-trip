{
  "name": "church-camp-trip",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site — no build step'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "googleapis": "^140.0.0",
    "nodemailer": "^8.0.7"
  }
}
